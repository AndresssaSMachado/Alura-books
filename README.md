# Alura-books
Trata-se de uma API que vai disponibilizar uma lista de objetos, pegando informações que estão na lista e exibindo informações na tela.

As habilidades utilizadas para confecção do referido projeto foram JS (métodos: filter, forEach, map, reduce e sort), HTML e CSS (que já constavam no projeto-base).
